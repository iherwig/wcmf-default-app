version = 1.0
build.info = build.info
dist.dir = ../dist

app.name = app

generator.base = ../vendor/olympos/chronos-generator
generator.workdir = ../model
generator.target = ../

dojo.build.node = false
dojo.build.base = ${basedir}/../${app.name}/public/vendor/dojo
dojo.build.profile = ../../../../../../build/app.profile.js
dojo.build.log = dojo-build.log
dojo.build.report = build-report.txt
